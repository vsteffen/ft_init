By using the command "traceroute slash16.org", we can see the network interface between us and servers behind slash16.org.
The network interface between local network and outside is "nat-1" (10.60.1.11).
